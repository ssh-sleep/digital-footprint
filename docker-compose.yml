version: '3.8'

services:
  jekyll:
    image: jekyll/jekyll:latest
    command: >
      sh -c "gem install webrick && jekyll serve --watch --force_polling --host 0.0.0.0"
    ports:
      - "4010:4000"  # Maps container 4000 to local 4010
    volumes:
      - .:/srv/jekyll